import {
FETCH_STREAM,FETCH_STREAMS,CREATE_STREAM,EDIT_STREAM,DELETE_STREAM
}from './types';

export default ( state =[], action) =>{
    switch (action.type){
        default:
            return state;
    }

}